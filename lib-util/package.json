{
  "name": "@azure-typescript-e2e-apps/lib-util",
  "version": "1.0.",
  "description": "Helpful utilities for these apps",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && npm run format && npm run lint && npm run build:tsc",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts --fix",
    "build:tsc": "tsc",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^20.2.1",
    "@typescript-eslint/eslint-plugin": "^5.59.6",
    "eslint": "^8.40.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.8.8"
  }
}
