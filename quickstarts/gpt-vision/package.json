{
  "name": "key-vault-quickstart",
  "version": "1.0.0",
  "scripts": {
    "image-prompts": "ts-node src/image-prompts.ts",
    "build": "npm run clean && npm run format && npm run lint && npm run build:tsc",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts --fix",
    "build:tsc": "tsc",
    "clean": "rm -rf dist",
    "prompts": "ts-node src/image-prompts.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.2",
    "@typescript-eslint/eslint-plugin": "^5.59.6",
    "eslint": "^8.40.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.8.8",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3",
    "wtfnode": "^0.9.1"
  },
  "prettier": {
    "printWidth": 80
  }
}
